<!DOCTYPE html>
<html lang="fr">
<?php include "../layout/metadata.php"; 
require "../src/managers/Buildmanager.php";?>

    <body>
   <div class="build_disclaimer">
           <p>Ceci n'est qu'une proposition de build, d'autres variantes existes </p>
   </div>
   
       <div class="build_layout">
               
         <?php


         $characterId = $_GET['characterId'];
         $bm = new Buildmanager();
         $build = $bm->getBuildByCharacterId($characterId);
         
         var_dump($build);
        var_dump($_FILES);
        $buildUrl="/uploads/".$build;
        var_dump($buildUrl);
        
         ?>      
        <img class="build_layout" src=uploads/Yelan_build.png alt="">
<?php

?>
               
       </div> 
  
</body>
</html>

